# Lab

Protocols, reagents, equipment notes.

Start adding pages here — one Markdown file per protocol or topic. Update the `nav:` in `mkdocs.yml` (or remove the `nav:` block entirely to let MkDocs auto-generate it from the folder).
